#include <vector>
#include <string>
#include <algorithm>

std::vector<std::string> usernames = {std::string("46009361")};

bool check_usernames(std::string username){
  return std::find(vec.begin(), vec.end(), item) == vec.end()
}
